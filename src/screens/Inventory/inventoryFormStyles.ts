import { formStyles } from '../Shared/Prepper/formStyles';

/**
 * Shared styles for inventory item forms (New and Edit screens).
 *
 * This file re-exports the shared inventory form styles to avoid duplication.
 */
export const inventoryFormStyles = formStyles;
