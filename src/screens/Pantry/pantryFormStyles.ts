import { formStyles } from '../Shared/Prepper/formStyles';

/**
 * Shared styles for pantry item forms (New and Edit screens).
 *
 * This file re-exports the shared pantry form styles to avoid duplication.
 */
export const pantryFormStyles = formStyles;
